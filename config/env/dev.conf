# ---------------------------------
# Development Configuration Section
# ---------------------------------

dev {

  # --------------------
  # Logger configuration
  # --------------------
  log {
    #receiver = "file"
    #level = "trace"
  }

  markdown {
    cache = false

    # Markdown domain host URL
    aah_domain_url = "http://localhost:8080"
    aah_docs_domain_url = "http://docs.localhost:8080"
  }

}
